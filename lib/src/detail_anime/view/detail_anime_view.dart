import 'package:flutter/material.dart';

class DetailAnimeView extends StatelessWidget {
  const DetailAnimeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
